<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
         <item name="android:windowIsTranslucent">true</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <!-- 设置主界面item索引的样式 -->
    <style name="setting_item_index_style">
        <item name="android:textSize">@dimen/text_size_30</item>
        <item name="android:textColor">@android:color/holo_blue_dark</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/setting_item_index_text_bg</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_alignParentLeft">true</item>
    </style>

    <!-- 设置主界面item的样式 -->
    <style name="setting_item_text_style">
        <item name="android:textSize">@dimen/text_size_30</item>
        <item name="android:textColor">@drawable/textcolor_selector</item>
        <item name="android:ellipsize">end</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">3.0dip</item>
        <item name="android:singleLine">true</item>
    </style>
    
    <!-- 日期设置的label样式 -->
    <style name="Settings_Date_Lable_Style">
        <item name="android:textSize">45.0sp</item>
        <item name="android:textColor">#ff555555</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- 日期设置的edittext样式 -->
    <style name="Settings_Date_Edittext_Style">
        <item name="android:textSize">45.0sp</item>
        <item name="android:textColor">#ff000000</item>
        <item name="android:ellipsize">middle</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/datetime_edit_bg</item>
        <item name="android:layout_width">0.0dip</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:maxLines">1</item>
        <item name="android:layout_weight">1.0</item>
    </style>
    
    <!-- 自定义对话样式 -->
    <style name="CustomDialogStyle" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:backgroundDimEnabled">true</item><!--模糊-->  
        <item name="android:backgroundDimAmount">0.6</item> 
     </style>
</resources>
